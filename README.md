# gh-deployment-workflow

This project demonstrates how to use GitHub Actions to deploy a simple `index.html` website to GitHub Pages.

## Features

- Deploys automatically to GitHub Pages
- Only runs when `index.html` changes
- Uses a workflow file in `.github/workflows/deploy.yml`